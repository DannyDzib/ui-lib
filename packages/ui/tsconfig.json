{
  "extends": "../../tsconfig.json", // Ruta al tsconfig.json global
  "compilerOptions": {
    "baseUrl": "." // Esto puede sobrescribir o extender configuraciones
  },
  "include": [
    "**/*.ts",
    "**/*.tsx"
  ]
}